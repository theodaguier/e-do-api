const currentSession = require("./currentSession");
const equipment = require("./equipment");
const clients = require("./client");
const coloramas = require("./colorama");

module.exports = {
  currentSession,
  equipment,
  clients,
  coloramas,
};
